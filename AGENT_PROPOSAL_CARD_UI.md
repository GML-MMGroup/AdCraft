# Agent Proposal Card UI

## Intent

方案卡是 Agent 创作过程中的一次明确选择入口。它需要让用户快速比较公开方案、确认引用素材，再提交一次结构化选择。本次改动只调整当前可操作的方案卡；历史方案卡继续保留原有的时间线回放样式。

## Layout

每张方案卡采用单层深色表面：

1. 顶部显示后端提供的能力名称和当前卡片语义，不显示内部 operation、skill 或模型字段。
2. 公开方案按后端顺序纵向排列。每行包含两位序号、标题和公开摘要；推荐项显示小型 `Recommended` 标记。
3. 当前选中项只通过左侧强调线、背景和 `aria-pressed` 表达，不额外显示 `Selected` 文本。
4. 有引用时，在方案下方显示引用素材托盘。必选引用明确标注 `Required`，可选引用保留 Include 控件；缩略图和输入角色继续来自后端数据。
5. 当前交互卡底部保留现有 Submit、Revise、Delegate、Defer、Exclude 等结构化操作。历史卡继续使用原有只读结构，不渲染操作按钮。

## Interaction rules

- 点击方案只更新本地选中状态，不自动提交。
- Submit 仍由现有 `ConceptChoiceSubmitControls` 触发，并携带当前 interaction revision、session revision 和 accepted references。
- 引用的删除、排序、必选/可选修改继续由现有引用提交逻辑负责。
- 历史方案不参与本次视觉改动，不恢复旧的“创建节点”或聊天文本操作。
- 方案数量和推荐状态完全以后端返回为准，前端不推断创作阶段。

## Visual constraints

- 当前方案卡不使用 ABC 字母圆圈、厚重渐变或卡片嵌套卡片；历史方案卡保留原有视觉。
- 选中态不显示文字标签，使用低对比蓝色边界和左侧强调线即可。
- 颜色保持黑、灰、低饱和蓝；成功、失败等状态仍由现有状态组件负责。
- 方案行的内容使用可扫描的两级文字层级，摘要最多由容器自然换行，不裁切用户可读内容。
